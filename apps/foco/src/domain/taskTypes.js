/** Vistas de lista agenda (Ahora / Luego). */
export const AGENDA_LIST_VIEWS = /** @type {const} */ (['ahora', 'luego']);

/** Tipos de tarea en Foco. */
export const TASK_TYPES = /** @type {const} */ (['TAREA', 'EVENTO']);
