import useEnsureRutinaForDate from '../../daily/useEnsureRutinaForDate';

/** @deprecated use useEnsureRutinaForDate from habits/daily */
export default function useCarouselRutinaBoot(targetDate) {
  useEnsureRutinaForDate(targetDate);
}
