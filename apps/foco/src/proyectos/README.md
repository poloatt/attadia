# Carpeta legacy `proyectos/`

**Deprecada.** Toda la UI vive en [`../objetivos/`](../objetivos/).

Los archivos aquí solo re-exportan componentes de `objetivos/` para compatibilidad con rutas antiguas (`/proyectos` redirige a `/objetivos` en `App.jsx`).

Importa siempre desde `../objetivos/...` en código nuevo.
