{
  "name": "atta-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && npm run build",
    "dev": "npm run dev",
    "start": "npm run start"
  },
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
